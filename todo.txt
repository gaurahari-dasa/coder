Jaya Srila Prabhupada!
Jaya Sri Sri Nitai Gauranga!

1. Add textarea option
2. Add SearchDown option
3. Add MultiSelect option
4. Add input type "time"
5. If the underlying DB column is non-null, Required option must be checked by default
6. 
7. When the UI has a file upload, the editForm must include a _method: "patch", entry and the method to send the data should be POST
8. When one of the fields is a file, on the server-side - store the file in the filesystem and then use that path for the database column value
9. For date-only input fields, you must apply date-only morph when outputting (displaying in the grid) 
10. Also apply date-time morphs **automatically** for fields of datetime input datatypes. User has to choose DateTimeColumn for outputting (displaying in grid).
11. Add set-focus attribute to Vue component tag (this has been missed out actually).
12. Add DateTime column to UI (Index.txt template).
13. Recursive table fetching is not apparently happening, Haribol.